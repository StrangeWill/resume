language: ruby
rvm:
  - 1.9.3
before_script:
  - npm install -g npm
  - npm install -g jekyll
  - npm install -g gulp
  - npm install
  - chmod -R +x ./bin/*
script: ./bin/build.sh
after_script:
  - ./bin/deploy.sh